# OpenCode 使用说明书

| 项目 | 内容 |
|------|------|
| 文档名称 | OpenCode 使用说明书 |
| 适用版本 | 本仓库归档快照（Go 终端 AI 编程助手） |
| 读者 | 开发者、DevOps、想把编码 Agent 接入脚本的用户 |
| 编制日期 | 2026-09-20 |

---

## 目录

1. [整体功能概述](#1-整体功能概述)  
2. [安装与首次启动](#2-安装与首次启动)  
3. [交互式 TUI 使用说明](#3-交互式-tui-使用说明)  
4. [非交互模式与脚本集成](#4-非交互模式与脚本集成)  
5. [会话、压缩与费用](#5-会话压缩与费用)  
6. [模型与提供商](#6-模型与提供商)  
7. [AI 工具详解](#7-ai-工具详解)  
8. [权限系统](#8-权限系统)  
9. [自定义命令](#9-自定义命令)  
10. [MCP 扩展](#10-mcp-扩展)  
11. [LSP 集成](#11-lsp-集成)  
12. [主题、外观与附件](#12-主题外观与附件)  
13. [配置说明（全字段）](#13-配置说明全字段)  
14. [环境变量一览](#14-环境变量一览)  
15. [数据目录与日志](#15-数据目录与日志)  
16. [从源码构建与开发](#16-从源码构建与开发)  
17. [限制、归档与替代项目](#17-限制归档与替代项目)  
18. [常见问题 100 个](#18-常见问题-100-个)  
19. [分角色、分场景的详细使用指南](#19-分角色分场景的详细使用指南)  
20. [安全使用细则](#20-安全使用细则给所有人)  
21. [故障排查步骤书](#21-故障排查步骤书)  
22. [配置场景示例](#22-配置场景示例)  
23. [提示词写作建议](#23-提示词写作建议针对本工具箱)  
24. [与相邻工具的协作方式](#24-与相邻工具的协作方式)  
25. [卸载与清理](#25-卸载与清理)  
26. [术语表](#26-术语表)  
27. [常见问题详尽解答补编](#27-常见问题详尽解答补编)  
28. [附录：推荐工作流备忘](#28-附录推荐工作流备忘)

---

## 1. 整体功能概述

OpenCode 是一个运行在开发者本机终端里的 **Agentic（具身工具调用）编程助手**。它不是“只能聊天的 API 客户端”，而是把大语言模型接到你的真实仓库上：模型可以搜索文件、阅读代码、应用补丁、执行测试、查询语言服务器诊断，并在你的授权下改动工作区。产品形态是 **单一 Go 二进制文件**，启动后进入全屏终端界面（TUI），也可以用一条命令把问题交给它并拿回纯文本或 JSON，方便写进 Makefile、CI 或个人脚本。

从使用者角度看，OpenCode 解决的是这类日常摩擦：你记得“认证逻辑大概在某个 middleware 里”，但不想手工 grep 十次；你想让模型按项目既有风格补一个接口，而不是把整文件贴进网页对话框；你希望改动发生在本地 Git 工作区，随时 `git diff`，而不是在云端沙箱里猜路径。它把“提问—推理—动手—再提问”这一循环留在你已经打开的终端标签页里，并尽量用会话、费用和权限把过程变得可管理。

把这四层合在一起，OpenCode 的产品承诺可以概括成：在开发者已经熟悉的终端里，用一次对话驱动多步工程动作，同时把危险动作暴露成必须点头的对话框，把过程留在本地数据库以便回顾。它不承诺替代设计评审、不承诺在无测试的情况下“一次改对”，也不承诺对提示注入免疫。把它用好的人，通常已经有清晰的 Git 习惯和阅读 diff 的耐心；缺少这两点时，助手只会把错误传播得更快。对团队而言，真正需要统一的不是每个人的快捷键偏好，而是：项目记忆写什么、哪些 MCP 允许出现在仓库配置、非交互模式能不能进 CI、以及代码必须经过人的眼睛才能合并。这些约定写进本说明书后面的场景章，请至少浏览标题。即便你只想“马上开始用”，也建议先读完第 8 章权限和第 20 章安全：工具默认站在开发者这一边加速工作，但不会替你承担把密钥写进仓库或把破坏性命令点成允许的责任。花十分钟建立习惯，比事后用 Git 考古省得多。

### 1.1 产品能力地图

可以把 OpenCode 理解成四层能力叠在一起。

**第一层是对话与会话。** 每一次你在编辑器里按下发送，内容会作为用户消息写入 SQLite。助手的思考、正文、工具调用和工具结果也落库。你可以同时保留很多根会话（例如“修登录 bug”和“写 README”分开），在对话框里切换。子任务会话（模型内部派出的探索 Agent）不会把列表弄乱，但费用会汇总到父会话。会话标题通常由一个小模型根据你的第一句话自动生成，所以列表里看到的不是 UUID，而是可读摘要。

**第二层是工具化的编码循环。** 模型并非只能“建议你去改某行”，它可以直接调用内置工具：`glob`/`grep`/`ls`/`view` 用于探索，`edit`/`write`/`patch` 用于修改，`bash` 用于测试与 Git，`fetch` 用于拉取文档，`sourcegraph` 用于搜公开仓库，`diagnostics` 用于读 LSP 报错，`agent` 用于把宽泛搜索外包给只读子 Agent。只要配置了 MCP 服务器，那些外部工具会以 `服务器名_工具名` 的形式出现在同一工具箱里。循环会一直进行，直到模型认为任务完成（或你按取消、或权限被拒绝）。

**第三层是人机共治。** 默认情况下，写文件、执行可能改系统状态的命令、访问网络、调用 MCP，都会弹出权限对话框。你可以允许一次、在本会话对同类操作持久允许、或拒绝。拒绝后，本轮尚未执行的工具会被取消，避免模型在你说“不行”之后继续连开十个 shell。非交互模式为了脚本可用性会自动批准——这是明确的权衡，只应在你信任提示词和工作区时使用。

**第四层是工程现场集成。** 你可以声明 LSP（如 `gopls`），让模型看见编译级诊断；可以放置 `OpenCode.md`、`.cursorrules`、`CLAUDE.md` 等文件作为“项目记忆”，它们会进系统提示；可以用 Markdown 自定义命令把“拉 issue + 读相关文件”变成 `Ctrl+K` 里的一项；可以选主题、换模型、看日志、附加图片。上下文将满时，默认会自动摘要压缩，让你不至于突然撞上模型的窗口上限。

### 1.2 典型一天怎么用

假设你在一个 Go 服务仓库的根目录打开终端。你先 `export ANTHROPIC_API_KEY=...`（或写进 `~/.opencode.json`），然后运行 `opencode`。第一次在该项目启动，可能会询问是否生成 `OpenCode.md`，你让它扫描目录结构、常用测试命令并写下来。接着你输入：“登录接口在 500，帮我定位并补测试。” 模型会 `grep` 路由、`view` 处理器、提出假设、`edit` 修改、`bash` 跑 `go test`。中途它要执行测试时弹出权限，你按 `a` 允许。底部状态栏的 token 百分比慢慢涨，费用按模型单价累计。你不满意某次方向，按取消，改口补充约束再发。完成后 `Ctrl+S` 切到别的会话去写发布说明。晚上用 `opencode -p "用三句话总结今天的 git diff 风格不要求"` 做个备忘——这条不会打开 TUI，直接打印。

另一种用法是纯只读咨询：不给写权限、不配 bash，只问“这段并发安全吗”。模型仍会 `view` 文件，但若它试图 `edit`，你一律拒绝，把它限制在顾问角色。OpenCode 没有单独的“问询模式”开关，但权限策略在实践中就能把角色分开。

第三种用法是团队约定：把 `.opencode.json` 里的 `lsp`、`mcpServers`、`contextPaths` 提交进仓库（不要提交 API Key），把自定义命令放进 `.opencode/commands/`，让每个人的助手遵循同一套“先看 OpenCode.md 再动手”的纪律。

### 1.3 和网页 Chat、IDE 插件的差异

网页聊天需要你手动贴代码，没有工作区真相，也没有一键跑测试。IDE 插件（包括后来的许多 Agent 产品）往往深度绑编辑器。OpenCode 走的是 **编辑器无关的终端路径**：Vim、Emacs、VS Code、甚至纯 SSH 远程开发都可以，只要有终端和 API 密钥。代价是界面限于字符栅格，复杂 diff 没有 GUI 那么精致，但并排 diff 与语法高亮已经内置。

它也不是远程云 Agent：默认不把你的仓库上传到专用虚拟机，工具在你的用户权限下本地执行。这意味着速度快、数据不出境（除了发给 LLM 提供商的提示内容），也意味着 **模型一旦被授权，能力等同于你在该目录敲命令**。请务必阅读权限与安全相关章节。

### 1.4 功能清单（概览）

- 交互式全屏 TUI（Charm Bubble Tea）与非交互 `-p`  
- 多会话、自动标题、侧边栏文件变更统计  
- 多提供商：OpenAI、Anthropic、Gemini、Copilot、Bedrock、Azure、Groq、OpenRouter、Vertex AI、xAI、本地 OpenAI 兼容端点  
- 内置工具：bash、edit、write、patch、view、ls、glob、grep、fetch、sourcegraph、diagnostics、agent  
- MCP（stdio 与 SSE）动态工具  
- LSP 诊断供模型使用  
- 权限：一次 / 本会话 / 拒绝  
- 自定义 Markdown 命令与 `$NAMED` 参数  
- 自动及手动会话压缩  
- 主题、帮助、日志页、外部编辑器、图片附件、`@` 路径补全  
- SQLite 持久化（默认项目下 `.opencode/opencode.db`）

### 1.5 你需要具备什么

- 能运行 Go 1.24 构建出的二进制的操作系统（Linux、macOS；Windows 需自行验证终端兼容性）  
- 至少一家 LLM 提供商的有效凭证  
- 对将要修改的 Git 仓库有写权限，并建议始终在 Git 管理下工作以便回滚  
- 基本的终端操作；不必会 Vim，但熟悉快捷键会快很多  
- 若要用诊断工具：安装对应语言服务器（如 `gopls`）  
- 若要用最快的 grep：安装 ripgrep（`rg`）——未安装时会回退到较慢的纯 Go 扫描  

### 1.6 设计上你应该建立的心智模型

把每次发送理解成 **启动一轮 Agent**，而不是“再聊一句”。这一轮可能包含十几次工具调用，历时数分钟。发送期间会话是“忙碌”的，不能再并发送第二句（会得到 session busy）。取消用聊天页的取消快捷键（实现为 `Ctrl+X`）。把“模型正在工作”当作编译过程：你可以去看侧边栏改了哪些文件，而不是同时插入另一段互相矛盾的指令。

另外请记住：模型看见的是 **工具返回的文本**，不是你的 IDE 缓冲区。未保存的编辑器内容它看不到。请从项目目录启动 OpenCode，并让磁盘上的文件成为真相来源。

### 1.7 归档说明对使用者的含义

本仓库已归档，官方后续在 Charm 的 Crush。若你为了学习、内网审计或冻结版本而使用本快照，本说明书按 **当前源码真实行为** 撰写。若与 README 个别快捷键或“压缩会新建 session”等描述冲突，以本说明书与源码为准。新功能请到 Crush 跟踪；本树适合作为稳定、可审计的本地助手。

以下各章按“你怎么操作”展开，尽量覆盖每一个功能入口。

---

## 2. 安装与首次启动

### 2.1 安装脚本

```bash
curl -fsSL https://raw.githubusercontent.com/opencode-ai/opencode/refs/heads/main/install | bash
VERSION=0.1.0 curl -fsSL https://raw.githubusercontent.com/opencode-ai/opencode/refs/heads/main/install | bash
```

脚本会按系统架构下载发布包并把 `opencode` 放到常见 PATH 目录。归档之后 URL 仍可能可用，但不再保证更新。

### 2.2 Homebrew / AUR / Go

```bash
brew install opencode-ai/tap/opencode
# Arch
yay -S opencode-ai-bin
# 从源安装到 GOPATH/bin
go install github.com/opencode-ai/opencode@latest
```

从源安装需要本机 Go 1.24+。

### 2.3 从本仓库构建

```bash
git clone https://github.com/opencode-ai/opencode.git
cd opencode
go build -o opencode
./opencode -v
```

### 2.4 第一次运行检查清单

1. 进入你的项目根目录（非常重要，工作目录决定工具默认搜索范围）。  
2. 配置至少一项 API Key（环境变量或配置文件，见第 13–14 章）。  
3. 运行 `opencode`。若立即退出并提示校验失败，通常是没有可用提供商或 `agents.coder.model` 指向了未启用的厂商。  
4. 可选：在配置里加上 `lsp.go.command = gopls` 或使用仓库自带的 `.opencode.json` 示例。  
5. 建议先提一个只读问题（“这个仓库是做什么的？”），确认模型能 `ls`/`view`，再交给它改代码。

### 2.5 命令行总览

```text
opencode                  交互模式
opencode -d               调试日志
opencode -c /path/proj    先切换到该目录再启动
opencode -v               打印版本
opencode -p "问题"        非交互，默认文本输出
opencode -p "问题" -f json
opencode -p "问题" -q     不要 spinner（脚本友好）
opencode -h               帮助
```

短选项与长选项：`--debug`、`--cwd`、`--prompt`、`--output-format`、`--quiet`、`--version`。

---

## 3. 交互式 TUI 使用说明

启动成功后进入交替屏幕（alt screen）。退出后终端恢复原样。界面通常包括：消息列表、底部编辑器、可选侧边栏（会话与改动文件）、最底部状态栏（模型、token、费用、LSP 错误数）。

### 3.1 全局快捷键（以源码 `internal/tui/tui.go` 为准）

| 快捷键 | 作用 |
|--------|------|
| `Ctrl+C` | 退出（可能先经过确认对话框） |
| `Ctrl+H` 或 `Ctrl+_` | 打开/关闭帮助（终端里常显示为 Ctrl+?） |
| `Ctrl+L` | 打开日志页 |
| `Ctrl+S` | 切换会话对话框 |
| `Ctrl+K` | 命令面板（内置 + 自定义命令） |
| `Ctrl+O` | 模型选择 |
| `Ctrl+T` | 主题选择 |
| `Ctrl+F` | 文件选择器（向即将发送的消息附加文件/图片） |
| `Esc` | 关闭当前遮罩/对话框 |

说明：早期 README 将切会话写成 `Ctrl+A`。当前实现是 **`Ctrl+S`**。若终端把 `Ctrl+S` 做成 XON/XOFF 流控，请用 `stty -ixon` 或改终端配置，否则会感觉“按了没反应”。

### 3.2 聊天页

| 快捷键 | 作用 |
|--------|------|
| `Ctrl+N` | 新建根会话 |
| `Ctrl+X` | 取消当前生成/工具执行 |
| `i` | 在非编辑状态时聚焦编辑器 |
| `Esc` | 编辑器失焦，焦点回到消息列表 |

发送一条消息的流程：聚焦编辑器 → 输入 → `Enter` 或 `Ctrl+S`（编辑器聚焦时源码以 `Ctrl+S` 发送，避免与多行输入冲突；未聚焦时 Enter 也可能发送，以帮助对话框实时列表为准）→ Agent 开始工作。消息列表会出现流式文本；若模型调用工具，会显示工具名与参数摘要，完成后展开结果（过长会截断显示）。

侧边栏会列出本会话 `history` 里记录过的文件及相对最初版本的增删行数，便于你用眼睛核对“它到底动了哪些文件”。

### 3.3 编辑器

- 支持多行。长提示建议 `Ctrl+E` 打开 `$EDITOR` 或 `$VISUAL`，保存退出后内容回到输入框。  
- 可用 `@` 触发路径补全，快速提到某个文件（补全数据来自工作区扫描）。  
- 附件：`Ctrl+F` 在文件树里选择。若当前模型 `SupportsAttachments` 为假，发送时附件会被丢弃，请换多模态模型。  
- 删除附件有专门按键绑定（见帮助页，避免和退格删文本混淆）。

撰写提示的建议：给出目标、约束、验证方式。例如：“在 `internal/auth` 修 JWT 过期边界；不要改 API 形状；跑 `go test ./internal/auth/...`。” 比“帮我修 bug”少很多无效工具调用。

### 3.4 会话对话框

`Ctrl+S` 打开后，`j/k` 或方向键移动，Enter 选中，Esc 关闭。列表只有根会话（没有 parent 的）。标题来自自动生成或创建时的默认字符串。非交互跑出来的会话标题带 `Non-interactive:` 前缀，也会出现在这里，便于事后回顾。

### 3.5 模型对话框

`Ctrl+O` 按提供商分页（流行度：Copilot、Anthropic、OpenAI…）。左右切提供商，上下切模型。选中后写入用户配置里的 `agents.coder.model` 并热切换。**Agent 正在忙碌时不能换模型**，需先 `Ctrl+X` 取消或等结束。

换模型不会自动改写历史里已产生的 assistant `model` 字段，只影响后续轮次。压缩用的 summarizer、标题用的 title agent 仍按其配置，不一定跟 coder 相同。

### 3.6 命令面板

`Ctrl+K` 列出：

- **Initialize Project**：生成或更新项目记忆文件（OpenCode.md 一类），把构建/测试命令、结构说明写进去，供以后的系统提示使用。  
- **Compact Session**：立刻把当前会话交给 summarizer 压缩（见第 5 章）。  
- 所有 `user:` / `project:` 自定义命令。

选中带 `$ARGS` 的命令会先弹出参数表，填完才发送。

### 3.7 权限对话框

当工具需要授权，界面中央出现说明（工具名、动作、路径、参数预览）。

| 键 | 行为 |
|----|------|
| `←` `→` 或 `Tab` | 在选项间移动 |
| `Enter` 或 空格 | 确认当前高亮选项 |
| `a` | 允许这一次 |
| `A` | 允许，并在本会话对相同工具、动作、目录记住 |
| `d` | 拒绝 |

拒绝会导致后续排在队列里的工具一并取消。请读第 8 章理解匹配粒度，避免误以为“允许了 bash 就允许了一切”。

### 3.8 日志页

`Ctrl+L` 进入表格，查看 slog 事件；可打开详情。调试 MCP/LSP 失败时先看这里。`Backspace` 或 `q` 返回聊天。若启动时加了 `-d`，级别更细。

### 3.9 帮助与退出

帮助对话框列出当前上下文相关的绑定（有对话框时绑定会变）。退出 `Ctrl+C`；若有未完成 Agent，可能需要先取消。

### 3.10 状态栏如何读

你会看到类似 token 数字、占上下文窗口的百分比、美元费用、LSP 错误/警告计数。百分比接近 95% 且未关闭 `autoCompact` 时，一轮结束后可能自动进入“Summarizing…”进度。费用是按模型目录里的每百万 token 单价估算，缓存读写有单独单价，实际账单以服务商为准。

### 3.11 消息列表里的“正在工作”

若存在尚未完成的工具调用，列表会显示工作指示与耗时。这不是卡住。极长的 `bash`（测试套件）可能跑到默认一分钟或你在工具参数里设的超时。超时后工具返回错误，模型可能会重试或改策略。

---

## 4. 非交互模式与脚本集成

```bash
opencode -p "Explain the use of context in Go"
opencode -p "列出 internal/llm 下的包" -f json
opencode -p "$TASK" -q -c /path/to/repo >> out.txt
```

行为要点：

1. 创建新会话，标题为 `Non-interactive:` 加提示前 100 字符。  
2. **自动批准该会话全部权限**。等价于你对所有写文件和 bash 点了同意。  
3. 等待 Agent 完全结束后，把 **最后一条助手文本**（不是工具日志）打印到 stdout。  
4. `-f json` 会包一层 JSON（实现见 `internal/format`），便于 `jq`。  
5. `-q` 关闭 spinner，避免污染管道。  
6. 退出码：启动失败、配置校验失败、Agent 错误为非零；用户取消上下文则可能记日志后以零结束（见 `RunNonInteractive` 对 `Canceled` 的处理）。

脚本建议：

- 用绝对 `-c` 锁定仓库，避免 cron 的 cwd 不对。  
- 把 API Key 放环境，不要写进被 `ps` 看到的命令行。  
- 对不可信的提示词不要用 `-p`（提示注入 + 自动批准 = 任意命令）。  
- 需要审计时把 `.opencode/opencode.db` 纳入备份，用后续会话对话框回看。

JSON 与 text 都只包含最终回答字符串，不包含中间 tool trace。若你需要轨迹，开 `OPENCODE_DEV_DEBUG=true`（见第 15 章）在数据目录落盘。

---

## 5. 会话、压缩与费用

### 5.1 会话是什么

会话是一组有序消息 + 可选文件快照 + 累计费用。根会话出现在切换列表。任务子会话 ID 等于工具调用 ID，`parent_session_id` 指向你正在看的会话。

数据库默认：`{data.directory}/opencode.db`，相对 **启动时的工作目录**。不同项目目录意味着不同数据库，这是有意的隔离。

### 5.2 新建与切换

无会话时直接发送会创建。`Ctrl+N` 显式新建。切换不会丢数据，只是 UI 焦点改变。删除会话（若 UI 暴露删除）会级联删除消息和文件快照。

### 5.3 自动标题

第一条用户消息发出后，后台用 `agents.title` 模型生成短标题（maxTokens 被强制为 80）。失败不影响对话，只是标题不好看。

### 5.4 压缩（Compact）

长对话会逼近 `ContextWindow`。OpenCode 的策略不是无限截断开头，而是：

1. 调用 summarizer 模型，提示它产出“有助于继续工作”的摘要（做了什么、正在做什么、哪些文件、下一步）。  
2. 把摘要存成 **同一会话** 里的一条助手消息。  
3. 设置 `summary_message_id`。  
4. 以后发给模型的历史从这条摘要开始，并把该条角色当作 user，以满足部分 API 约束。

手动压缩：命令面板 Compact Session。自动压缩：默认 `autoCompact: true`，当状态栏使用的 token 之和 ≥ 窗口 95% 且一轮刚结束时触发。进度通过 Agent 的 summarize 事件显示。

**实践建议：** 压缩后模型对早期细节的记忆会变糊。对关键约束，请写进 `OpenCode.md` 或在压缩后再用用户消息重申。不要把压缩理解成 Git 提交。

### 5.5 费用

状态栏美元来自模型表的单价估算。子 Agent 的花费会加到父会话。压缩本身也消耗 summarizer 的 token 并累加 cost。换更便宜的 title/summarizer 模型是常见优化。

---

## 6. 模型与提供商

### 6.1 配置思路

每个逻辑 Agent（`coder` / `task` / `title` / `summarizer`）指向一个 `model` ID（如 `claude-3.7-sonnet`），该 ID 在内置目录里绑定提供商与 API 模型名。你必须让对应 `providers.<name>` 有密钥且 `disabled` 不为 true。

若你不写 `agents`，启动时会按本机探测到的凭证，依流行度顺序选默认模型。

### 6.2 提供商与凭证（摘要）

| 提供商 | 典型环境变量 | 备注 |
|--------|----------------|------|
| Anthropic | `ANTHROPIC_API_KEY` | Claude 系列，支持附件与推理 |
| OpenAI | `OPENAI_API_KEY` | GPT 与 o 系列 |
| Gemini | `GEMINI_API_KEY` | Google AI Studio |
| Copilot | `GITHUB_TOKEN` 或 IDE 登录留下的 json | 实验性；需 Copilot Chat 资格 |
| Groq | `GROQ_API_KEY` | 速度快，模型集不同 |
| OpenRouter | 配置里 apiKey | 聚合多家 |
| Bedrock | `AWS_ACCESS_KEY_ID` 等 | 目录含 Claude 3.7 |
| Azure | `AZURE_OPENAI_ENDPOINT` 等 | 可用 Entra ID |
| Vertex AI | `VERTEXAI_PROJECT` + `VERTEXAI_LOCATION` | 云上 Gemini |
| Local | `LOCAL_ENDPOINT` 如 `http://127.0.0.1:1235/v1` | OpenAI 兼容 |

### 6.3 GitHub Copilot 特别说明

需要账号已开启 IDE 内 Copilot Chat。凭证可来自 VS Code / `gh` / Neovim 插件写入的 `~/.config/github-copilot/hosts.json` 或 `apps.json`，或显式 token。支持模型列表见 README，实际能否调用还取决于你的 Copilot 套餐。失败时看日志里 token exchange 错误。

### 6.4 本地模型

```bash
export LOCAL_ENDPOINT=http://localhost:1235/v1
```

配置：

```json
{
  "agents": {
    "coder": {
      "model": "local.your-model-id",
      "reasoningEffort": "high"
    }
  }
}
```

模型 ID 必须存在于启动时发现的本地目录（`internal/llm/models/local.go` 的逻辑）。工具调用质量强烈依赖模型是否训练过 function calling；小模型常常乱调用，此时应减少工具或改用云端 coder、本地仅作 title。

### 6.5 Reasoning / Thinking

对支持 `CanReason` 的 OpenAI 兼容模型，可设 `reasoningEffort` 为 `low`/`medium`/`high`。Anthropic 的 coder 在可推理模型上可启用 thinking。TUI 会把思考过程显示为独立区域（若提供商流式下发）。思考 token 通常按输出计费，注意成本。

---

## 7. AI 工具详解

你不直接点选这些工具，但理解它们能让你写出更好的提示，并在权限框里做出正确判断。

### 7.1 `glob` — 按文件名找文件

参数：`pattern`（必填，如 `**/*.go`）、`path`（可选）。结果按修改时间排序，最多约 100 个，可能截断。适合“测试文件在哪”，不适合搜文件内容。

### 7.2 `grep` — 搜内容

参数：`pattern`、`path`、`include`（如 `*.go`）、`literal_text`。默认当正则。搜字面量含很多符号时，应让模型用 `literal_text=true`。优先调用系统 `rg`。隐藏文件默认跳过。

### 7.3 `ls` — 目录树

参数：`path`、`ignore` 数组。跳过点文件和 `__pycache__`。大目录会截断。用来建立空间感，不是递归读所有文件。

### 7.4 `view` — 读文件

参数：`file_path`、`offset`、`limit`。带行号。大文件应分段读。图片会识别类型，是否真正作为多模态内容取决于模型。

### 7.5 `edit` — 精确替换

参数：`file_path`、`old_string`、`new_string`。`old_string` 必须在文件中 **恰好出现一次**，否则失败。空 `old_string` 表示创建；空 `new_string` 表示删除那段内容。这是最常用的安全改法。若模型反复 edit 失败，多半是空白不一致，让它先 `view` 再带更多上下文。

### 7.6 `write` — 整文件覆盖

适合新文件或重写。覆盖已有文件风险高，权限框里会看到 diff 类信息时请仔细看。

### 7.7 `patch` — 结构化补丁

用于较大 hunk。内部有模糊匹配（去行尾空格、再去全部空格）。仍可能因上下文漂移失败。失败时宁可让模型改用多次 `edit`。

### 7.8 `bash` — 持久 Shell

同一会话（更准确：同一工作目录单例 shell）里 cwd 与环境变量保持。默认超时 60 秒，最大 10 分钟。输出超过 30000 字符截断。

**禁止命令名**包括：`alias`、`curl`、`wget`、`nc`、`telnet`、浏览器等。这不是完整安全沙箱，模型仍可能用 python 访问网络。只读白名单包括许多 `git status/log/diff`、`go test` 等，用于减少无害命令的打扰。

提示模型时：明确测试命令；禁止它 `git push`（系统提示也如此要求）；提交信息若由它生成会带 OpenCode 署名模板。

### 7.9 `fetch` — HTTP GET

参数：`url`、`format`、`timeout`。可把 HTML 转 Markdown。会要权限。小心把内网 URL 或带 token 的地址交给它。

### 7.10 `sourcegraph` — 公开代码搜索

在公共索引里搜用法示例。需要网络。私有代码搜不到。

### 7.11 `diagnostics` — LSP 诊断

在配置了 LSP 且服务器已推送诊断时，模型可查询报错。改完代码后让它再跑一次诊断或 `go test` 是好习惯。LSP 能力远不止诊断，但本快照只把诊断暴露给模型。

### 7.12 `agent` — 只读子 Agent

参数：`prompt`。子 Agent 只有 glob/grep/ls/view/sourcegraph，不能改文件。适合“这个错误字符串在哪定义的”。父模型应把结果摘要给你，而不是让你阅读子 Agent 全文。子 Agent 费用计入父会话。

### 7.13 MCP 工具

命名：`{mcpServers 的键}_{远端工具名}`。权限动作一般为 `execute`。行为完全取决于外部服务器（数据库、浏览器、工单系统等）。见第 10 章。

---

## 8. 权限系统

每次敏感工具调用，服务端（本机进程）创建一条 `PermissionRequest`，TUI 订阅后弹窗。匹配“本会话允许”的键是：

- 同一个 session ID  
- 同一个工具名  
- 同一个 action（如 execute、写文件）  
- 同一个 **目录**（请求里的 path 会取 `filepath.Dir`）

因此：允许对 `/proj/internal/foo.go` 的编辑，并不自动允许 `/proj/cmd/main.go`（目录不同）。允许 `bash` 在项目根执行，不意味着允许 MCP。

非交互会话在创建后被加入 auto-approve 列表，跳过弹窗。

没有点对话框时，工具调用会一直阻塞——看起来像“卡住”。请不要以为模型在思考，去看看是否有被终端尺寸挡住的弹窗。

---

## 9. 自定义命令

### 9.1 放置位置

| 位置 | 前缀 |
|------|------|
| `$XDG_CONFIG_HOME/opencode/commands/` 或 `~/.opencode/commands/` | `user:` |
| `<项目>/.opencode/commands/` | `project:` |

文件必须是 `.md`。`git/commit.md` → `user:git:commit`。内容会作为 **用户消息** 发给当前会话的 Coder Agent，不是本地宏展开后直接执行（但内容里可以写“RUN …”“READ …”之类自然语言指令，由模型去调工具）。

### 9.2 命名参数

占位符必须匹配 `\$[A-Z][A-Z0-9_]*`，即美元符 + 大写字母开头。`$ISSUE_NUMBER` 合法，`$issue`、`$1ID` 不会被识别。同一名字出现多次只问一遍。发送前全部替换。

示例 `~/.config/opencode/commands/issue.md`：

```markdown
请查看 GitHub issue $ISSUE_NUMBER：
- 用 gh 拉标题和正文
- 在仓库里搜相关代码
- 给出修复计划，未经允许不要改代码
```

### 9.3 使用步骤

`Ctrl+K` → 选命令 → 填参 → Enter。确保当前会话是你想操作的那个。

---

## 10. MCP 扩展

在配置 `mcpServers` 中声明。`type` 为 `stdio` 时启动外部进程；为 `sse` 时连接 URL，可附 Headers。

启动时 OpenCode 会 Initialize 并 ListTools（30 秒超时）。工具列表缓存到进程结束，**改配置需重启**。每一次调用会再建立连接，因此 stdio 服务器若启动很慢，你会感觉每个 MCP 工具都卡一下。

安全：把 MCP 当作完整的第三方程序。它可以读你给它的环境变量。不要在 `env` 里塞生产数据库密码到不可信服务器。权限框仍会弹出，但请认真看参数。

---

## 11. LSP 集成

```json
"lsp": {
  "go": { "command": "gopls" },
  "typescript": {
    "command": "typescript-language-server",
    "args": ["--stdio"]
  }
}
```

注意结构体 JSON tag：字段在 Go 里叫 `Disabled`，tag 却是 `"enabled"`——填写时请对照 schema 与实验，避免把开关写反。`debugLSP: true` 可在日志中看到协议级噪声。

语言服务器在后台拉起，工作区文件变更会通知它。模型侧目前主要用诊断。若 gopls 没装，启动不会完全失败，但该客户端会出错，诊断工具为空。

---

## 12. 主题、外观与附件

内置主题名：`opencode`、`catppuccin`、`gruvbox`、`monokai`、`dracula`、`flexoki`、`onedark`、`tokyonight`、`tron`。`Ctrl+T` 选择后写入配置 `tui.theme`。

Markdown 渲染使用 Glamour；diff 使用 Chroma。终端必须支持 256 色或真彩，否则主题会难看但不影响 Agent。

图片附件依赖模型多模态能力。文件选择器可隐藏点文件。请勿附加巨大二进制或密钥文件。

---

## 13. 配置说明（全字段）

### 13.1 查找顺序

1. `$HOME/.opencode.json`  
2. `$XDG_CONFIG_HOME/opencode/.opencode.json`  
3. `$HOME/.config/opencode/.opencode.json`  
4. 当前工作目录 `./.opencode.json`（merge 覆盖）  

环境变量前缀 `OPENCODE_`（Viper AutomaticEnv）。例如与 debug 相关的键可被环境覆盖。API 密钥既可写在 `providers.*.apiKey`，也可只用官方厂商自己的环境变量（`ANTHROPIC_API_KEY` 等，见 `setProviderDefaults`）。

建议在项目文件里加 `"$schema": "https://.../opencode-schema.json"` 或仓库内相对路径 `./opencode-schema.json`，以便编辑器补全。重新生成 schema：`go run ./cmd/schema`。

### 13.2 完整示例与字段释义

```json
{
  "$schema": "./opencode-schema.json",
  "data": { "directory": ".opencode" },
  "providers": {
    "anthropic": { "apiKey": "", "disabled": false },
    "openai": { "apiKey": "", "disabled": false },
    "copilot": { "disabled": false }
  },
  "agents": {
    "coder": { "model": "claude-3.7-sonnet", "maxTokens": 5000, "reasoningEffort": "medium" },
    "task": { "model": "claude-3.7-sonnet", "maxTokens": 5000 },
    "title": { "model": "claude-3.5-haiku", "maxTokens": 80 },
    "summarizer": { "model": "claude-3.5-haiku", "maxTokens": 4096 }
  },
  "shell": { "path": "/bin/zsh", "args": ["-l"] },
  "mcpServers": {
    "example": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "some-mcp-server"],
      "env": ["FOO=bar"],
      "url": "",
      "headers": {}
    }
  },
  "lsp": {
    "gopls": { "command": "gopls", "args": [], "options": {} }
  },
  "tui": { "theme": "opencode" },
  "contextPaths": [],
  "debug": false,
  "debugLSP": false,
  "autoCompact": true
}
```

| 字段 | 含义与建议 |
|------|------------|
| `data.directory` | 相对工作目录的数据夹，含数据库与调试日志。默认 `.opencode`。请加入 `.gitignore` 若不想提交本地会话。 |
| `providers.*.apiKey` | 密钥；空则尝试环境变量。 |
| `providers.*.disabled` | true 则即使有密钥也不用。 |
| `agents.*.model` | 必须是 schema 枚举 / SupportedModels 中的 ID。 |
| `agents.*.maxTokens` | 单次生成上限，不能超过窗口；title 启动后会被强制 80。 |
| `agents.*.reasoningEffort` | OpenAI 推理强度。 |
| `shell.path/args` | bash 工具用的登录壳。CI 里可设 `bash` 与 `[]` 避免读很重的 zshrc。 |
| `mcpServers` | 见第 10 章。 |
| `lsp` | 见第 11 章。 |
| `tui.theme` | 主题名。 |
| `contextPaths` | 覆盖默认列表则需自己列全；默认已包含 OpenCode.md、CLAUDE.md、.cursorrules 等。 |
| `debug` / `debugLSP` | 日志级别与 LSP 噪声。 |
| `autoCompact` | 95% 窗口自动摘要。 |

项目级配置适合提交 `lsp` 与 `commands`，不适合提交密钥。可用环境变量在 CI 注入。

### 13.3 默认 contextPaths

若你不写该字段，系统会尝试读取：`.github/copilot-instructions.md`、`.cursorrules`、`.cursor/rules/` 目录、`CLAUDE.md`、`CLAUDE.local.md`、以及多种大小写的 `opencode.md`。文件内容进入系统提示，相当于“永久记忆”。不要把密钥写进这些文件。

---

## 14. 环境变量一览

除第 6 章提供商变量外：

| 变量 | 作用 |
|------|------|
| `SHELL` | 未配置 `shell.path` 时的默认壳 |
| `LOCAL_ENDPOINT` | 本地 OpenAI 兼容 API 根路径（通常含 `/v1`） |
| `GITHUB_TOKEN` | Copilot / gh |
| `OPENCODE_DEV_DEBUG` | 为 `true` 时把 slog 写到 `{data.directory}/debug.log`，并把请求轨迹写到 `{data.directory}/messages` |
| `XDG_CONFIG_HOME` | 影响配置与 github-copilot 文件位置 |
| `EDITOR` / `VISUAL` | `Ctrl+E` 外部编辑器 |
| Azure/AWS/Vertex 一整套标准云变量 | 见 README 表格 |

Viper 还会映射 `OPENCODE_` 前缀到配置键，具体嵌套键以 Viper 的扁平化规则为准（调试时用 `-d` 观察是否生效）。

---

## 15. 数据目录与日志

默认 `{cwd}/.opencode/`：

- `opencode.db`：会话、消息、文件版本  
- `debug.log`：仅开发调试开关打开时  
- `messages/`：每轮请求/工具 JSON，便于复盘  

TUI 日志页展示进程内 slog，不一定等于 debug.log。崩溃时 `RecoverPanic` 会尝试记录。数据库损坏时，先备份再删库会丢失会话，但可重新开始。

文件快照可能包含源码全文，注意磁盘加密与备份范围。

---

## 16. 从源码构建与开发

前置：Go 1.24+。`go test ./...` 运行现有单元测试（覆盖 LS、主题、命令占位符、contextPaths）。改 SQL 后安装 sqlc，执行 sqlc generate。发版看 `.goreleaser.yml` 与 `scripts/release`。

贡献流程见 README：fork、分支、PR。归档仓库可能不再接受功能 PR。

---

## 17. 限制、归档与替代项目

- 无操作系统级沙箱；权限依赖你点击与 bash 黑名单。  
- 工具串行，大范围探索偏慢。  
- MCP 列表不热更新。  
- 压缩与 token 统计是启发式。  
- Windows 终端、部分快捷键可能冲突。  
- 项目已归档，新发展见 [Crush](https://github.com/charmbracelet/crush)。  

---

## 18. 常见问题 100 个

**Q1：OpenCode 是什么？**  
A：本地终端里的 AI 编程 Agent，能读改你的仓库并调用工具，而不是纯聊天网页。

**Q2：必须用 Anthropic 吗？**  
A：不必。有密钥的提供商即可，启动时会按流行度选默认模型。

**Q3：没有 API Key 能启动 TUI 吗？**  
A：配置校验通常会失败。至少配置一个提供商。

**Q4：免费吗？**  
A：客户端 MIT，模型调用按各厂商账单收费。本地模型只耗你的机器。

**Q5：会把整个 Git 历史上传吗？**  
A：不会自动打包仓库。只有进入提示、工具结果和附件的内容会发给当前 LLM 提供商。

**Q6：数据存在哪？**  
A：默认项目下 `.opencode/opencode.db`。

**Q7：如何换项目？**  
A：在对应目录运行，或 `-c` 指定。不同目录不同数据库。

**Q8：`Ctrl+S` 没反应？**  
A：可能被终端流控占用，执行 `stty -ixon`；切会话也是这个键。

**Q9：README 写 Ctrl+A 切会话？**  
A：以当前源码 `Ctrl+S` 为准。

**Q10：如何取消生成？**  
A：聊天页 `Ctrl+X`。权限等待中需先处理对话框。

**Q11：提示 session busy？**  
A：同一会话不能并行两轮。等结束或取消。

**Q12：如何新建会话？**  
A：`Ctrl+N`，或对空状态直接发送。

**Q13：子任务会话为什么列表里没有？**  
A：`ListSessions` 过滤了有 parent 的行，避免噪音。

**Q14：标题乱码或空？**  
A：title 模型失败或返回空，不影响对话，可忽略或换 title 模型。

**Q15：如何换模型？**  
A：`Ctrl+O`，或编辑配置 `agents.coder.model` 后重启（对话框会热更新 coder）。

**Q16：忙碌时不能换模型？**  
A：是的，源码明确拒绝，防止中途切换提供商。

**Q17：附件没发出去？**  
A：当前模型不支持附件时会被丢弃。换多模态模型。

**Q18：如何用外部编辑器写长提示？**  
A：聚焦编辑器后 `Ctrl+E`。

**Q19：`@` 补全不到某文件？**  
A：可能被当隐藏文件跳过，或不在工作区扫描范围。用绝对路径写在提示里。

**Q20：中文输入异常？**  
A：依赖终端 IME；尝试原生终端或关掉某些 multiplex 的异常按键转换。

**Q21：颜色很怪。**  
A：换主题 `Ctrl+T`，或换支持真彩的终端。

**Q22：如何看快捷键？**  
A：`Ctrl+H` / 帮助对话框。

**Q23：日志在哪看？**  
A：`Ctrl+L`；更深则 `-d` 或 `OPENCODE_DEV_DEBUG`。

**Q24：非交互如何静默？**  
A：`-q`，再重定向 stdout。

**Q25：`-f json` 的结构？**  
A：由 `formatAsJSON` 包装字符串，用 `-p` 对简单提示试一次即可看到字段。

**Q26：非交互会改文件吗？**  
A：会，如果模型决定 edit/write/bash，且自动批准已打开。

**Q27：如何让非交互只读？**  
A：本快照没有只读开关；不要用 `-p` 做危险任务，或在提示中严令不要修改并接受它仍可能尝试（但你无法在无 TUI 时拒绝）。只读需求请用交互模式拒绝写工具。

**Q28：autoCompact 关掉？**  
A：配置 `"autoCompact": false`。

**Q29：压缩后模型“失忆”？**  
A：早期细节只剩摘要。把稳定约束写入 OpenCode.md。

**Q30：压缩会开新会话吗？**  
A：不会。同一会话设置 `summary_message_id`。README 有过时说法。

**Q31：95% 阈值用的什么 token？**  
A：会话上记录的最近一轮 prompt+completion 估算，不是完美的真实占用。

**Q32：费用准吗？**  
A：按内置单价估算，缓存价也可能与账单不同。

**Q33：如何降低费用？**  
A：小模型做 title/summarizer；少贴大文件；及时压缩；能用 grep 就别让它把整库 view 一遍。

**Q34：支持 Groq 吗？**  
A：支持，需 `GROQ_API_KEY` 并选对应模型 ID。

**Q35：如何用 Azure？**  
A：设 endpoint、版本、密钥或 Entra，模型 ID 用 Azure 表里的那些。

**Q36：Bedrock 区域不对？**  
A：设 `AWS_REGION` 且账号已开通对应 Claude 模型。

**Q37：Vertex 报权限错误。**  
A：检查 ADC/`VERTEXAI_PROJECT`/`LOCATION` 与模型是否在该区域开放。

**Q38：Copilot 一直失败。**  
A：确认 Chat 资格、hosts.json 是否存在、token 是否过期；看 copilot token exchange 日志。

**Q39：本地 Ollama 怎么接？**  
A：用 OpenAI 兼容代理地址设 `LOCAL_ENDPOINT`，保证含 `/v1`，模型 ID 与发现逻辑一致。

**Q40：reasoningEffort 无效？**  
A：只有部分 OpenAI/本地可推理模型会读这个字段。

**Q41：模型胡乱调工具。**  
A：换更强 coder 模型；在 OpenCode.md 写清何时用 bash；拒绝异常权限。

**Q42：为什么禁止 curl？**  
A：降低提示注入导致的数据外带；请用 `fetch` 工具并经你批准。

**Q43：模型说要用 curl 怎么办？**  
A：让它改用 fetch 或你自己在终端执行后把结果粘贴给它。

**Q44：bash 超时？**  
A：默认约 60s。让模型在参数里提高 timeout，上限 10 分钟。测试套件太长请你本地跑。

**Q45：bash 输出被截断。**  
A：超过约 3 万字符。让模型写到文件再 `view` 尾部。

**Q46：cd 之后下一轮又不对？**  
A：持久 shell 其实会保持 cwd；但系统提示不鼓励 cd。尽量让命令用绝对路径。

**Q47：edit 总是失败。**  
A：old_string 必须唯一且完全一致（含空格）。先 view。

**Q48：edit 改错了地方。**  
A：上下文不够唯一。`git checkout -- 文件` 回滚后要求更大上下文。

**Q49：write 覆盖了整文件！**  
A：看权限 diff；用 Git 恢复。教育模型优先 edit。

**Q50：patch 失败。**  
A：上下文模糊匹配仍失败。改用 edit 分步。

**Q51：grep 不到内容。**  
A：是否在隐藏目录；是否要 `literal_text`；include 是否把文件排除了。

**Q52：没有安装 rg？**  
A：会回退纯 Go 扫描，更慢但不致命。建议安装 ripgrep。

**Q53：ls 看不到点文件。**  
A：设计如此。需要时在提示里给出精确路径让 view。

**Q54：glob 结果不全。**  
A：上限约 100，按 mtime。缩小 pattern。

**Q55：fetch 要不要开代理？**  
A：走系统 HTTP 代理环境变量（若 Go 默认 transport 尊重它们）。公司网需自配。

**Q56：sourcegraph 搜不到私有代码。**  
A：只能搜它索引的公开代码。

**Q57：diagnostics 永远空。**  
A：未配 LSP、语言服务器未起、或文件尚未被打开/监视。安装 gopls 等并配置。

**Q58：agent 工具改了我的文件？**  
A：按设计不能，它没有 edit/bash。若文件变了，是父 Agent 自己的工具。

**Q59：MCP 工具没出现。**  
A：检查 type/command、启动日志、30s 超时、是否需重启。

**Q60：MCP 每次调用都很慢。**  
A：每次冷启动 stdio 进程。换常驻服务器或忍受延迟。

**Q61：如何给 MCP 传 Token？**  
A：stdio 的 `env` 或 SSE 的 `headers`。注意别提交到 Git。

**Q62：两个 MCP 工具名冲突？**  
A：前缀是配置键，换键名即可。

**Q63：LSP `enabled` 字段很迷。**  
A：Go 字段 Disabled 的 json tag 为 enabled，填写后用一次 gopls 是否真启动来验证。

**Q64：debugLSP 日志太大。**  
A：用完关掉。不要在生产会话一直开。

**Q65：权限选了 A 下次还问。**  
A：目录、工具名或 action 不同。只对四元组精确匹配。

**Q66：权限拒绝后模型停了。**  
A：后续工具被取消。重新发消息并说明不要用被拒工具。

**Q67：弹窗被挡住。**  
A：拉大终端；Esc 关闭其它 overlay。

**Q68：非交互没有权限提示。**  
A：自动批准。这是功能不是 bug。

**Q69：自定义命令不显示。**  
A：检查目录、`.md` 后缀、重启（若实现为启动时扫描，改文件后重开面板试试）。

**Q70：`$foo` 没被替换。**  
A：必须大写开头 `$FOO`。

**Q71：命令里想写美元金额？**  
A：避免 `$` 加大写标识符；否则会被当成参数。

**Q72：Initialize Project 做了什么？**  
A：引导模型写项目记忆文件，方便以后的系统提示。

**Q73：OpenCode.md 不生效。**  
A：确认在工作目录且文件名在 contextPaths 默认列表中；改完可能需重启（实现里有 Once 缓存）。

**Q74：如何加自己的规则文件？**  
A：配置 `contextPaths` 加入路径。覆盖时记得保留你仍需要的默认文件。

**Q75：密钥写进 OpenCode.md 了怎么办？**  
A：立刻轮换密钥，从文件和 Git 历史清除。

**Q76：`.opencode` 要不要提交？**  
A：不要提交数据库和日志；可以提交 `commands/` 和去密钥的 `.opencode.json`。

**Q77：多用户共用数据库？**  
A：不支持。SQLite 按工作目录本地用。

**Q78：如何备份会话？**  
A：复制 `opencode.db`。注意其中可能有源码快照。

**Q79：数据库锁错误。**  
A：不要同时开两个 OpenCode 写同一 db。关掉多余进程。

**Q80：升级后 db 不兼容？**  
A：goose 会跑迁移。先备份。本快照迁移很少。

**Q81：TUI panic。**  
A：有恢复逻辑并 Quit。用 `-d` 复现；报告终端类型与窗口大小。

**Q82：SSH 里快捷键冲突。**  
A：tmux/ssh 抢了 Ctrl 组合，改外层前缀或在本地跑。

**Q83：Windows 能用吗？**  
A：非一等支持。路径、shell、信号可能有问题。WSL 更稳。

**Q84：SELinux/AppArmor 禁了它？**  
A：二进制需能起子进程（bash、lsp、mcp）。调策略或在开发机用。

**Q85：公司代理 MITM HTTPS。**  
A：把 CA 导入系统信任，否则 SDK 校验失败。

**Q86：如何让它提交 Git？**  
A：明确请求；它会 status/diff/log。注意它被提示不要 push、不要改 git config。提交前你自己审查。

**Q87：它推送到 remote 了？**  
A：系统提示禁止，但不能物理阻止。权限上盯 bash。用 Git hook 或拒绝异常命令。

**Q88：如何只让它改某个目录？**  
A：本快照无路径 ACL。在提示中约束，并对目录外的权限按 d。

**Q89：并发两个 Agent 改同一仓库？**  
A：两进程会打架。一次一个工作区，或用不同 Git worktree。

**Q90：CI 里怎么跑？**  
A：`-p -q -c`，注入密钥，接受自动批准风险，用干净 runner。

**Q91：和 Crush 什么关系？**  
A：本项目归档，Crush 是原作者与 Charm 的后续。新用户优先评估 Crush。

**Q92：还能提功能 PR 吗？**  
A：归档仓可能只读。fork 自行维护或去 Crush。

**Q93：schema 和真实配置不一致？**  
A：以运行时 `config.Validate` 为准；可重新 `go run ./cmd/schema`。

**Q94：maxTokens 设很大报错。**  
A：不能超过模型窗口。减小或换大窗口模型。

**Q95：如何关闭某个提供商？**  
A：`"disabled": true`，即使环境里有 Key。

**Q96：主题名写错？**  
A：回退默认并打 warn 日志。用 Available 列表里的名字。

**Q97：帮助里的键和实际不一样？**  
A：以帮助对话框动态列表和源码 key binding 为准，本说明书已按源码校正切会话键。

**Q98：模型一直循环同一工具。**  
A：取消后告诉它结果已足够；换模型；检查工具是否总返回截断导致它重试。

**Q99：怀疑提示注入（网页/MCP 让它执行危险命令）。**  
A：拒绝权限；关掉对应 MCP；不要用 `-p` 喂不可信文本；审查 fetch 内容。

**Q100：出了问题如何完整复现给维护者？**  
A：提供 `opencode -v`、操作系统、提供商与模型 ID、是否 `-d`、脱敏后的日志、最小仓库步骤、相关配置（删掉密钥）。归档项目请到 fork 或 Crush 社区。

---

## 19. 分角色、分场景的详细使用指南

本章把前面分散的功能重新按“你实际要完成的工作”串起来，便于对照操作。文字尽量具体，避免只重复快捷键表。

### 19.1 刚接手一个陌生仓库

目标是在不破坏任何文件的前提下，让助手帮你建立心智模型。启动前先 `git status` 确认干净。进入仓库根目录（有 `go.mod`、`package.json` 或语言清单的那一层，而不是某个深层子目录），运行 `opencode`。若弹出初始化对话框，可以允许它生成 `OpenCode.md`，但生成后你必须亲自打开该文件：删除猜测错误的技术栈描述，补上真正的测试命令、代码生成命令、以及“绝对不要动”的目录（例如厂商 SDK、生成代码）。这些句子会进入系统提示，比你在每一轮对话里重复约束更省 token。

第一轮提问建议完全只读，例如：“用目录树和 README 说明这个服务如何启动、测试怎么跑、配置从哪读。不要修改任何文件。” 然后盯着权限框：如果它仍然申请 `edit` 或非只读 `bash`，直接拒绝，并补一句“继续只使用 ls/view/grep”。读完它的总结后，用你已知的事实纠正它。纠正内容同样建议写进 `OpenCode.md`，否则压缩或新会话后还会犯同样的错。

陌生仓库上最常见的浪费是：模型从根目录无脑 `ls`，再对每个看起来重要的文件 `view` 全文。你应在提示里给出入口线索（“从 cmd/root.go 和 internal/app 开始”），或先自己用 `@` 补全贴出两三个关键路径。Grep 工具有 100 个文件上限，提示里写清符号名会比“找一下认证”更有效。

### 19.2 修复一个可复现的缺陷

把缺陷当成实验而不是聊天。提示结构建议固定为四段：现象、复现命令、你已经排除的原因、验收标准。例如：“`go test ./internal/session` 中 TestX 失败，日志是……；我已确认不是数据库文件锁；请补最小修复并让该包测试通过；不要改 API。” 这样模型会倾向于先 `bash` 跑失败测试，再 `grep` 测试名，再 `view` 实现。

权限方面：第一次跑测试时用 `A` 把项目根目录的 bash 记住，避免每跑一次测试弹一次窗。但不要对 `fetch` 或 MCP 使用会话级允许，除非你很清楚参数。修复完成后不要让它直接 `git commit`，除非你已经 `git diff` 过。系统提示会诱导它写带 OpenCode 署名的提交信息；若团队不接受机器人联名，在提示里明确禁止提交。

若测试套件超过一分钟，默认 bash 超时会切断。这时有两种做法：你在宿主终端跑测试，把失败片段贴进对话；或明确让模型把 timeout 提高到合理毫秒数（上限十分钟）。超时后模型有时会误判“测试失败”，你会看见它开始改无关代码——此时应 `Ctrl+X` 取消并纠正。

### 19.3 新增功能（跨多个文件）

先开新会话，避免旧缺陷讨论污染上下文。若功能较大，第一句话只要求计划：“列出将修改的文件、对接口的影响、测试策略，先不要改代码。” 看完计划再发“按计划执行，每改一处就跑相关测试”。OpenCode 没有独立的 Plan 模式开关，这个两阶段是靠你的提示和权限（计划阶段拒绝 write）模拟的。

跨文件时侧边栏会逐渐出现多个路径和增删行。把它当作核对表：出现了你没批准的路径，立刻取消并回滚该文件。`history` 快照是给 UI 做 diff 统计的，不是 Git 替代品；回滚仍然用 Git。

上下文涨到状态栏 80% 以上时，主动命令面板压缩，或干脆新开会话并把计划和已改文件列表用用户消息重新贴一次。自动压缩触发点在约 95%，那时往往已经开始丢细节。

### 19.4 代码评审与讲解

把 OpenCode 当评审助手时，用 Git 范围约束：“只看 working tree 相对 HEAD 的 diff，不要修改文件，指出并发、错误处理和测试缺口。” 模型会 `git diff`（只读白名单内）和 `view`。你仍须拒绝任何 write。讲解类任务（“给新人讲清楚 agent 循环”）同样保持只读，并要求引用文件与行号；本快照不会做可点击跳转，但行号足够你在编辑器里定位。

### 19.5 文档与提交说明

让它根据 `git log` 与 diff 写 CHANGELOG 或 PR 描述是低风险任务，但仍建议在提示中禁止它执行 `git commit` 和 `git push`。生成的 Markdown 你可以复制走，不必让它直接写入仓库，除非你确实想把文件落盘。

### 19.6 与自定义命令配合的团队节奏

把团队重复劳动写成 `project:` 命令，例如“按我们的规范做 code review”“从 issue 号拉上下文”。参数用 `$ISSUE` 这种全大写。命令文件里写清禁止事项（不要改 go.mod、不要格式化无关文件）。成员只需 `Ctrl+K`，减少每个人提示词风格不同导致的行为差异。用户级命令适合个人习惯（“用我喜欢的方式总结 diff”），不要把私人 token 写进项目命令。

### 19.7 MCP 实际使用注意

以“给助手接上工单系统”为例：stdio 服务器一旦能读环境变量里的 API Token，就具备和你本人相同的工单权限。启动后先用一个无害只读 MCP 工具试探，确认权限框展示的参数符合预期。若服务器启动要下载 npm 包（`npx -y`），第一次会很慢，ListTools 的 30 秒超时可能直接让工具列表为空——这时先在普通终端跑通该命令，再开 OpenCode。SSE 类型要把公司代理和证书问题先解决，否则 Initialize 失败只会在日志页出现一条错误，TUI 看起来“一切正常只是没工具”。

### 19.8 LSP 诊断驱动的修复循环

配置 gopls 后，改代码往往需要一点时间才刷新诊断。模型若立刻调用 `diagnostics` 可能拿到旧结果。较好的提示是：“edit 之后跑 `go test`，若失败再看 diagnostics。” 语言服务器崩溃不会让 OpenCode 退出，但状态栏计数不准；日志页会有启动失败。多个 LSP 可以并存，分别服务不同语言，模型侧诊断工具会聚合。

### 19.9 非交互模式在 Makefile 中的用法

适合生成样板解释、更新不自动提交的草稿、或在受控环境做仓库问答。不适合作为 CI 里“自动修复全部测试”的无人值守步骤，除非 runner 是一次性的并且你接受仓库被改乱。调用时始终 `-q -c "$(CURDIR)"`，把提示放在文件里通过命令替换传入，避免在进程列表暴露超长字符串。输出用 `-f text` 对人类友好，用 json 时注意转义。失败要用 make 的错误码中断，不要把半截回答提交进产物。

### 19.10 多会话并行思考（单进程内）

单进程里可以对不同根会话依次工作：在 A 会话取消或结束后 `Ctrl+S` 切到 B。不要期望两个会话同时跑 Agent——实现上按会话互斥，且 bash 持久壳按工作目录单例，两路写入会交错。真正的并行请用两个 Git worktree 加两个进程，并指向不同的 `data.directory`，以免抢同一 SQLite 文件。

---

## 20. 安全使用细则（给所有人）

把 OpenCode 的权限模型记成一句话：**对话框挡住的是“这一次工具调用”，不是“这个模型今天的品德”。** 模型在下一轮可以换一种命令绕过你刚刚拒绝的 curl，比如用解释器发网络请求。黑名单只覆盖常见下载器名字，不是安全边界。因此：

生产密钥、云账号、内网管理接口，不要出现在工作区里让 `view` 能读到，更不要出现在 context 文件。若仓库已有 `.env`，在提示中明确禁止读取，并准备在权限框看到该路径时拒绝。fetch 和 MCP 都可能成为出站通道，会话级允许等于打开通道直到会话结束。

项目里的 `.opencode.json` 能合并进全局配置。克隆不可信仓库后先看这个文件是否声明了陌生 MCP 或把 shell 指到恶意脚本。本快照没有“项目配置必须二次确认”的机制，信任分级要靠你自己。

非交互自动批准等于把提示词作者变成 root 级操作员。来自工单、邮件、网页的文本不要直接 `-p` 喂进去。交互模式下，对看不懂的 bash 字符串一律拒绝，要求模型拆成可读命令。

日志和数据库会保存工具参数与文件快照，可能含源码和错误信息中的敏感数据。拷贝 `.opencode` 目录到 U 盘或贴到 issue 前必须脱敏。`OPENCODE_DEV_DEBUG` 会写更完整的请求 JSON，日常请关闭。

---

## 21. 故障排查步骤书

遇到问题时按层缩小范围，避免一上来就删数据库。

**第一步：确认工作目录和二进制。** `pwd` 是否为仓库根；`which opencode` 是否为你以为的版本；`opencode -v` 记录下来。用 `-c` 显式指定往往能消除“怎么搜不到文件”的问题。

**第二步：确认提供商。** 用 curl 或官方 SDK 示例直接调一次 API，排除密钥、额度、代理、证书。OpenCode 校验失败信息有时只说 provider disabled 或 model not supported，需要对照 `SupportedModels` 的 ID 字符串，而不是网页上的营销名。

**第三步：看 TUI 日志页和 debug。** `-d` 启动。MCP、LSP、权限、Agent 取消都会打点。若界面“卡住”超过十秒且没有流式输出，先找权限弹窗，再找 bash 是否在跑测试，再找网络。

**第四步：工具层。** grep 异常时安装 rg 再试；edit 失败就把那段 old/new 拷出来在编辑器里搜是否真的唯一；bash 失败看是黑名单还是超时还是非零退出。

**第五步：数据层。** 配置错误改 json；会话错乱则新开会话；数据库损坏才备份后删除 `opencode.db`。不要先删配置再删库，否则更难对比。

**第六步：终端层。** tmux、SSH、中文输入法、`Ctrl+S` 流控、窗口过小导致 overlay 画不出来，都表现为“快捷键失灵”。直接退出用 `Ctrl+C`，必要时另开一个真正的 TTY。

若疑似模型进入空转（同一 grep 参数重复很多遍），取消后告诉它“停止搜索，根据已有结果下结论”，或换更强模型。这不是配置错误，是策略问题。

---

## 22. 配置场景示例

### 22.1 个人笔记本（Anthropic 为主，本地模型写标题）

全局文件放 Anthropic 密钥，coder 用 Claude，title 和 summarizer 用便宜或本地模型以省钱。shell 指向你日常的 zsh 登录壳，这样 bash 工具能继承 pyenv 等环境。不要在全局启用一堆 MCP，按项目开。

### 22.2 纯 Go 服务仓库

项目 `.opencode.json` 只声明 gopls。`OpenCode.md` 写明 `go test ./...`、lint 命令、生成代码目录禁止手改。自定义命令放“新增 HTTP 接口骨架”之类。data.directory 保持默认并 gitignore。

### 22.3 前端仓库

配置 typescript-language-server。提醒模型使用 glob 限制在 `src`，不要把 `node_modules` 列入搜索（ls 默认不跳过 node_modules 目录名本身，提示里要写 ignore 或让它不要 ls 进去）。测试若是 `npm test` 交互式，改为明确的 CI 脚本并提高 timeout。

### 22.4 内网离线

使用 `LOCAL_ENDPOINT` 指向内网推理网关。关闭所有需要公网的提供商。不要配置 sourcegraph 和 fetch，或在提示中禁止。MCP 仅连内网 stdio。注意本地小模型工具调用格式经常不稳，需要网关做 prompt 适配。

### 22.5 CI 一次性 runner

只使用 `-p -q`，密钥来自 CI secret。checkout 后工作目录即仓库。接受它改文件则在后续步骤 `git diff --exit-code` 做门禁，或明确只允许问答。并发 job 各有自己的 checkout，不会共享 db。

---

## 23. 提示词写作建议（针对本工具箱）

写给 OpenCode 的提示与写给网页 Chat 不同，因为对方真的会执行。有效提示通常包含：范围（哪些目录）、动作（只读还是允许改）、验证（哪条命令算成功）、禁止（不要提交、不要动生成代码）、已知事实（接口已存在于某文件）。

把“聪明的搜索”留给工具名：你想找定义就说“用 grep 精确匹配函数名，literal 模式”，想找文件就说 glob。不要说“全面扫描整个公司的所有仓库”，它只有当前工作目录和可选的 sourcegraph 公网搜索。

一次只交一个主目标。把重构、改依赖、写文档拆成不同会话。模型在同一轮并行发出多个工具时，本快照仍会顺序执行，提示里要求“先搜再改”能减少它未读文件就 write 的概率。

发现它在瞎猜 API，立刻取消，贴上你 `view` 过的关键片段或让它必须先 view 再下结论。系统提示已经要求不要猜，但小模型经常违反。

中文提示完全可用。若模型在提交信息里混用中英，在 OpenCode.md 规定语言。工具名和路径保持真实大小写，Windows 用户尤其注意。

---

## 24. 与相邻工具的协作方式

OpenCode 不取代 Git、编辑器、CI。推荐闭环是：编辑器负责你精细打磨的代码，OpenCode 负责探索和机械修改，Git 负责历史，CI 负责最终真理。不要同时在 IDE 里改未保存缓冲区又让 OpenCode 按磁盘内容 edit，否则必然覆盖。保存、再让 Agent 动手、再在 IDE 里 reload。

需要图形 diff 时，用你的 GUI 去看工作区，不必等 TUI 的并排 diff 展示所有细节。TUI diff 适合快速扫一眼增删行数量。

若你同时使用网页版大模型，不要把 OpenCode 的数据库当同步通道。需要跨工具共享的是 `OpenCode.md` 和测试命令，而不是会话 JSON。

---

## 25. 卸载与清理

二进制可能在 `/usr/local/bin/opencode` 或 Go 的 bin 目录。删除即可。每个用过的项目目录下会留 `.opencode/`，按需删除。主目录的 `~/.opencode.json`、`~/.config/opencode/`、`~/.opencode/commands/` 是用户配置与命令。卸载不会撤销已经提交的 Git 变更，也不会作废已产生的 API 账单。

---

## 26. 术语表

- **Agent / 助手循环**：模型反复调用工具直到结束本轮。  
- **Coder**：主对话使用的 Agent 配置。  
- **Task Agent**：只读子助手。  
- **会话 Session**：SQLite 里的对话容器。  
- **压缩 Compact**：摘要后截断发给模型的历史。  
- **权限请求**：写/执行类工具前的同步确认。  
- **MCP**：把外部工具以标准协议接进来。  
- **LSP**：语言服务器，本产品主要用诊断。  
- **提供商 Provider**：实际 HTTP API 的厂商。  
- **模型 ID**：配置里写的逻辑名，不是一定等于厂商 API 名。  

---

## 27. 常见问题详尽解答补编

下面把第 18 章的百问按主题再讲一遍，每条补充操作细节、原因和变通办法，便于检索时只看这一章也能独立解决问题。

### 27.1 产品与账户

关于产品定位：OpenCode 把大语言模型接到本机文件系统和终端上，核心价值是“能动手的对话”，而不是更漂亮的聊天皮肤。它适合已经用 Git 管理的代码仓库，不适合当通用知识问答网站。若你主要需要检索公开资料，网页模型可能更合适；若你需要改本地代码并跑测试，才值得把密钥配进这个二进制。归档并不等于立刻不能用，只表示不要期待官方继续修新功能。若团队要长期依赖，应评估 Crush 或自行 fork。

关于厂商选择：没有“必须用哪一家”。质量、速度、价格、是否支持工具调用和附件，才是选择依据。同一仓库里可以把 coder 设成强模型，把 title 设成便宜模型。凭证可以只放环境变量，配置文件留空 apiKey，这样截图或分享 json 时不容易泄漏。若校验报模型不支持，先核对逻辑 ID 是否写错，例如少了前缀或用了网页上的显示名。

关于免费与账单：客户端开源免费，每次工具循环都可能产生多轮 API 调用，费用是对话轮数乘以每轮输入输出，外加思考 token 和缓存。侧边栏或状态栏的美元只是内置单价估算，折扣、企业合约、缓存实际命中率都以控制台为准。本地模型把费用变成电费和显存，但工具调用成功率通常更差，需要你更多人工盯权限。

关于隐私：发给提供商的内容包括系统提示、项目记忆文件、历史消息、工具返回值。这意味着 grep 到的源码片段、bash 测试输出、报错里的路径都会出境。数据库和调试日志留在本地。没有“企业私有部署 LLM”的内置方案，除非你用 LOCAL_ENDPOINT 指向内网网关。不要在提示里粘贴客户数据或未公开密钥。

### 27.2 启动、目录与界面

工作目录决定一切默认路径：搜索根、配置 merge、数据库位置、LSP 工作区。在家目录直接运行会导致它扫描你的整个 home，既慢又危险。用 `-c` 或先 `cd`。多个 git submodule 时，在你真正要改的那个模块根启动，或在提示里写死绝对路径。

`Ctrl+S` 被终端吞掉是经典问题：许多 Unix 终端把该组合当成停止向屏幕输出。表现是按键“没了”，过一会儿又突然刷出很多字。`stty -ixon` 可以关掉这套流控。切会话与发送在不同焦点下都可能用到这个组合，帮助对话框会显示当前绑定。若你改不了终端，就多用鼠标点列表（若终端支持）或命令面板完成同等操作。

会话忙碌是硬互斥，不是礼貌提示。硬要并发送第二条，API 会返回错误而不是排队。正确做法是取消或等。取消会把进行中的工具标成用户取消，模型这一轮以错误结束；你的半成品文件改动不会自动回滚，必须自己看 Git。

新建会话不会自动携带旧会话的文件快照语义到模型上下文，但磁盘上的代码是共享的。所以“新开会话继续改同一功能”完全可以，只要你把目标和关键文件在第一句重新说清。标题生成失败只影响列表美观。非交互会话也会出现在列表里，标题带固定前缀，可以当运行记录看，但默认不会帮你打开当时的工具轨迹，除非开了开发调试落盘。

附件被丢弃发生在模型声明不支持附件时，界面不一定有大红报错。发图片前先在模型对话框确认选的是多模态型号。图片会进上下文，又大又贵，无关截图不要附。外部编辑器依赖环境变量，若 `Ctrl+E` 立刻失败，检查容器或 SSH 会话里有没有设置 EDITOR。

`@` 补全跳过隐藏路径，所以 `.github/workflows` 可能补不到。直接把路径写进提示即可，view 工具并不禁止点目录，只是补全器偷懒。中文输入在某些终端会把组合键打断，表现为半个拼音被发送；改用支持 IME 的终端模拟器，或先在外部编辑器写完再贴回。

颜色异常通常仍能用。真正的功能问题是窗口太小，对话框和侧边栏重叠，权限按钮看不见。把终端放到一百二十列以上再试。日志页是排查 MCP 和 LSP 的第一现场，不要等失败很久才去翻。

### 27.3 非交互、压缩与钱

非交互模式为脚本设计，因此自动批准。这不是疏忽。如果你把不可信字符串插进 `-p` 参数，就等于允许字符串作者在你的用户权限下跑命令。只读需求在本快照没有开关：要么交互着拒绝写工具，要么在隔离的虚拟机和一次性目录里跑。JSON 输出只包最终助手文本，做链路追踪不够，需要开发调试目录里的 JSONL。

压缩不是备份。它把模型窗口腾出来，代价是细节丢失。自动阈值用的是会话上被覆盖写入的近一轮 token 字段，所以有时你会觉得“明明聊了很久却还没压缩”，或反过来“才两轮就压了”。不放心就手动压缩或新会话。压缩过程中也会占忙碌锁。摘要本身会消耗 summarizer 模型费用。把长期有效的约束写成项目记忆，比依赖摘要更可靠。

省钱的方法依次是：减少无谓的全文 view、限制搜索路径、用小模型做标题、关掉不必要的 MCP 冷启动、及时结束空转循环、能本地 grep 的你自己先 grep。换更便宜的 coder 会同时降低代码质量，未必真省，因为失败重试会吃更多轮次。

### 27.4 模型、工具与权限

Azure、Bedrock、Vertex、Copilot 失败多半是云侧权限而不是 OpenCode 写错字段。先在云控制台调用同一模型。Copilot 还依赖 Chat 资格和本机已有的登录文件，企业禁用 IDE Chat 时这里也会失败。本地网关必须提供 OpenAI 风格的 chat completions 与 tool calling，缺一块就会出现“只会说话从不调工具”或“胡乱编工具名”。

bash 黑名单拦的是命令名片段，不是网络能力。把它当成防呆，不是沙箱。超时和输出截断都会让模型误判。长测试请你自己跑。持久壳会记住 cd 和 export，这既方便又危险：某一轮 `export SECRET=` 可能影响下一轮。尽量要求绝对路径，少用 cd。edit 失败几乎总是空白和唯一性，把文件在编辑器里打开对比比让模型再猜三次更快。write 覆盖无法在工具内部撤销。patch 适合大块，但上下文漂移时不如多次小型 edit。

grep 要注意正则与字面量。搜 `foo.bar` 当正则会匹配 fooXbar。让模型开 literal_text。未安装 ripgrep 时第一次全库扫描会慢，不是死锁。ls 隐藏点文件导致它看不见 `.env`，这是保护也是坑：你若希望它配置环境示例，要把文件名写出来。diagnostics 空常常只是 gopls 没装或还没 index 完。子 Agent 不能写文件，若你看见写入，那是父 Agent 干的。

MCP 列表空：命令不存在、超时、Initialize 失败、type 填错。每次调用冷启动导致慢，这是实现限制，重启或减少服务器数量。权限四元组按目录匹配，允许 `internal/foo` 不等于允许 `cmd`。拒绝会取消队列里剩下的工具，所以不要在模型一次性发出五个写操作时只想拦第五个——前面四个可能已经执行完。卡住时先找弹窗。

自定义命令是“把这篇 Markdown 当用户消息发出去”，不是宏引擎。模型仍要去调工具。占位符规则严格，小写不行。Initialize Project 会让模型写记忆文件，写完必须人审。contextPaths 有进程内一次缓存，改完文件不重启可能仍用旧内容，这是已知行为。密钥进记忆文件等于把密钥当系统提示发给模型厂商，必须轮换。

数据库按项目隔离，不要幻想做团队云同步。锁错误来自双进程。备份 db 等于备份源码快照，按机密文件保管。TUI panic 后先换终端大小和主题再报 bug。SSH 与 tmux 抢快捷键时，把 OpenCode 跑在内层并给外层换前缀。Windows 请用 WSL。强制访问控制要允许派生子进程。公司 HTTPS 检查要信任中间人证书，否则所有提供商一起挂。

Git 提交可以做但应人审。push 被提示禁止，仍可能被模型尝试，靠权限和 hook。没有目录 ACL。两个 Agent 同时写同一工作树几乎必然冲突。CI 用一次性机器。Crush 是后续产品。schema 只是编辑器辅助。maxTokens 过大校验失败。disabled 可关掉环境里的密钥对应厂商。主题名错误会回退。空转时取消并换策略。提示注入时把外部文本当数据而不是指令，并拒绝可疑工具。复现问题要版本、模型、脱敏日志和最小仓库。

### 27.5 百问逐条扩写（操作向）

**再答安装与启动：** 安装脚本在归档后可能指向旧发布包，校验 checksum 失败就改用 `go install` 或本地 `go build`。Homebrew tap 也可能停更。构建需要 Go 1.24，旧发行版编译器会直接拒绝 go.mod。第一次启动若立刻退出，把命令换成 `opencode -d` 看校验器说的是缺密钥还是模型 ID。配置文件是 JSON，末尾多逗号会导致读入失败，用编辑器的 schema 校验。

**再答会话管理：** 删除会话会级联删消息，不可从回收站恢复，只有 db 备份能救。列表按创建时间倒序。任务子会话在库里能查到但 UI 不展示，不必惊慌。切换会话不会取消另一会话里正在跑的 Agent——若你切走时它仍忙碌，回来时可能已经改完文件。养成先看状态栏是否 busy 再切的习惯。

**再答编辑器发送：** 多行编辑时误触发送很烦。看帮助里“编辑器聚焦时发送键是什么”。用外部编辑器写长文更稳。粘贴超大日志前先截断，否则一轮输入本身就占满窗口。发送空消息没有意义，界面可能仍创建用户气泡，浪费一次标题生成。

**再答模型热切换：** 只换 coder。summarizer 仍按配置文件，除非你编辑 json。换模型后历史还在，但不同厂商对 tool_use 历史的格式兼容性不同，极少数情况下一轮会报消息格式错误，这时新开会话并把结论粘过去。

**再答主题与帮助：** 主题存在配置里，换机器要带上 json。帮助内容随当前是否打开对话框而变，先关掉权限框再看全局键。日志页返回键是 Backspace 或 q，不要在日志页狂按 Ctrl+C 除非你想退出整个程序。

**再答 grep/glob 上限：** 截断时工具结果会提示 truncated。让模型加 include 或换目录再搜，而不是假定“没有更多匹配”。对生成目录、vendor、node_modules 明确忽略，否则 100 个名额会被依赖库占满。

**再答 fetch：** 会把 HTML 转文本或 Markdown，脚本和样式应被剥掉，但仍可能带跟踪参数 URL。内网管理地址不要让它去抓。超时参数过小会失败重试，过大会让你以为卡住。

**再答 sourcegraph：** 需要出站网络，对国内网络或不稳定代理会失败。它不能登录你的私有实例（本快照未见相关配置）。把它当“看看别人怎么调用这个 API”，不能当安全审计唯一来源。

**再答 shell 配置：** 登录壳会读 zshrc，可能又慢又有交互提示把持久 shell 搞挂。CI 或容器里把 args 改成非登录、非交互。路径必须存在，否则 bash 工具全部失败。

**再答数据目录改名：** 改 `data.directory` 等于换库，旧会话不会自动搬迁。先关进程，移动目录，再改配置。不要指向网络盘上的 SQLite，锁和延迟都很痛。

**再答调试开关：** `OPENCODE_DEV_DEBUG` 会在数据目录写 messages 和 debug.log，可能包含完整提示。用完关掉并删除这些文件再打包项目。普通 `-d` 只提高 slog 级别，相对更安全。

**再答并发与 worktree：** 每个 worktree 有独立路径，就能有独立 `.opencode` 数据库，这是本机并行的正确方法。记得在提示里写清当前 worktree 路径，避免模型用绝对路径改到主工作树。

**再答卸载残留：** 删除二进制后，编辑器里的 schema 引用、shell 别名、CI 步骤仍可能调用 `opencode`。全局配置不会自动删。密钥在环境变量里与本程序无关，卸载不会撤销。

以上补编与第 18 章一一对应，若两处有出入，以源码行为及本章更长的解释为准。

### 27.6 每日检查清单（可打印）

早晨打开仓库时：确认分支、确认工作区没有别人或昨天 Agent 留下的半成品、确认磁盘上的 `.opencode.json` 没有被上游偷偷加入新的 MCP。启动后看状态栏模型是否仍是你想用的那个。提问前用一句话写清验收命令。结束前自己跑一遍测试，而不是完全相信模型说“已经通过”。离开座位时不要留下一个打开的、已对本会话永久允许 bash 的 TUI，以免路过的人或恶意网站内容通过你的粘贴板进入提示。下班前看一眼 Git，提交必须出自己的名字和大脑。

### 27.7 给团队培训的十分钟讲稿

OpenCode 是终端里的同事，不是编译器。它读得到磁盘，跑得了测试，也会在你点允许之后改文件。我们把它当加速器：搜代码、写样板、解释陌生模块。我们不把它当发布按钮：禁止它 push，禁止它改密钥，禁止在生产机器上跑。每个人配自己的 API 密钥，项目只提交去密钥的 json 和 commands。权限对话框不是装饰，看不懂就拒绝。会话和数据库不要当知识库备份，真正要共享的写进 OpenCode.md 和测试。出了错用 Git 回滚，不要跟模型吵架超过三轮还不 `Ctrl+X`。归档项目意味着新功能去 Crush 看，本快照负责稳定。问问题先看使用说明书第 18 章和本章补编，再打断别人。

### 27.8 结束语

本使用说明书尽量覆盖每一个功能入口、每一项配置和一百个真实会遇到的问题。工具会演进，快捷键可能再变，但“在 Git 管理的目录里小步委托、对写操作保持怀疑、用测试和 diff 验收”这一用法不会过时。把助手当作能力很强、记忆很短、服从工具结果的实习生，你就会用得既快又相对安全。


### 27.9 按文件类型委托任务时的额外提醒

改 Go 代码时，优先让模型跑包级测试而不是整库测试，节省超时。生成代码目录（sqlc 输出、协议生成文件）应在 OpenCode.md 写明禁止手改，否则它会“顺手格式化”导致巨大 diff。改前端时禁止它重写 lockfile，除非任务就是升级依赖。改基础设施即代码时，必须人审，因为 bash 里一条 apply 就可能碰到真集群——本快照没有生产保护。改文档和注释风险最低，适合熟悉产品手感。改构建脚本要小心它引入网络下载。无论哪种语言，完成后你都应用语言自己的格式化工具再看一眼 diff，避免把模型的空格风格提交进去。

遇到二进制、图片资源、证书文件，明确禁止 view 之外的写入。模型把图片当文本打开会得到无意义字符还浪费窗口。数据库迁移文件要按团队编号规则新增，而不是改历史迁移。权限框里一旦出现仓库以外的绝对路径，默认拒绝，除非你正在让它改全局编辑器配置且你真的想改。

### 27.10 与安全团队沟通时可用的说明

若公司要评估本工具上线，可以这样描述攻击面：进程以开发者本人身份运行；能执行任意命令（经弹窗或非交互自动批准）；能读工作区文件并发送到第三方 LLM；能通过 MCP 和 fetch 出站；本地 SQLite 保存会话与源码快照；配置可由项目内文件覆盖。缓解措施包括：禁止生产机安装、强制 Git、禁用非交互、审查项目 json、限制 MCP 白名单、密钥放环境不放仓库、定期轮换、培训拒绝看不懂的命令。它不是经过沙箱认证的企业 Agent，评估时应按“增强版脚本引擎”而不是“只读聊天窗”。本说明书不构成安全承诺。

### 27.11 长任务中途停电或杀掉进程之后

没有检查点恢复。进行中的 bash 可能留下半写文件。重启后会话还在，最后一条助手消息可能停在未完成的工具调用。不要继续在同一条“未完成轮次”上假设状态一致，应新发一条：“刚才进程被杀，请 git status 看工作区，回滚不完整修改，再从某某文件继续。” 先让它只读检查，你自己决定是 `git checkout` 还是接着改。数据库一般不会因为杀进程而损坏，SQLite WAL 会恢复；若损坏，用备份。持久 shell 会随进程消失，环境变量不保留到下次启动。

### 27.12 教学场景下的用法

给新人演示时，选一个玩具仓库，关掉非交互，打开 debug 让大家看工具参数。故意拒绝一次权限，展示取消后续工具的行为。再演示 compact 前后回答质量的差异。不要用含真实客户数据的仓库做培训。强调状态栏费用，避免新人开着最贵模型无限制探索。演示结束删除 `.opencode` 以免把示例密钥快照留下来。

把以上各节与前面的功能章对照阅读，即可在不看源码的情况下完成几乎全部日常操作。若仍有缺口，以 `1DESIGN.md` 的实现描述为准进行排障。


### 27.13 从零到第一个成功修复的完整例子

假设仓库是本项目自己。你在根目录导出 Anthropic 密钥，运行 `opencode`。帮助里确认切会话键。新建会话后输入：“只读说明 `internal/llm/agent/agent.go` 里 Run 如何防止会话并发，引用关键变量名。” 模型会 view 该文件并回答 `activeRequests` 与 `ErrSessionBusy`。你核对后另发：“请在注释中用中文补一句说明该互斥范围是每个 sessionID，不要改逻辑，改完用 `gofmt`。” 权限框出现 edit，你看 diff 只有注释，按 `a`。然后你自己 `git diff` 确认。若它顺手改了格式或无关函数，`git checkout -- 文件` 并拒绝再给写权限。这个最小闭环覆盖了只读探索、精确提示、权限阅读、Git 验收，比一上来让它“重构整个 agent”安全得多。把这个例子当成团队的上机第一题即可。

### 27.14 配置改坏了如何自救

现象包括启动秒退、TUI 起来但发消息报 provider 错误、主题消失、MCP 全无。把项目 `.opencode.json` 临时改名，只保留用户主目录配置再启动，用于判断是项目文件作祟还是全局文件作祟。JSON 语法错误时用 `python -m json.tool` 检查。模型 ID 从 schema 枚举里抄，不要手打。禁用全部 providers 只开一个，避免默认选择跳到你没额度的厂商。恢复时不要同时删数据库，否则你还会以为是会话丢了。实在混乱，备份后只留环境变量密钥、空配置，让程序按探测逻辑重建默认 agents。

### 27.15 你应当定期做的维护

每月轮换 API 密钥并检查配置文件是否被提交。清理过大的 `opencode.db` 和 debug 日志。更新语言服务器版本。复查 MCP 服务器是否还需要。阅读 Git 历史里是否出现过机器人提交且未人工审。检查 `stty` 与终端模拟器升级有没有把快捷键又改坏。若仍使用本归档快照，关注依赖库的安全公告，因为不会再有官方发版。维护时间不多，但能避免“某一天突然不能用了还不知道是密钥过期还是模型下线”。把这些动作写进团队日历，比出事后翻百问更有效。


### 27.16 最后一组实践口诀

先目录后启动，先只读后写入，先计划后动手，先测试后声称完成，先看 diff 后提交，先拒绝看不懂的命令，先备份再删库，先人审再 MCP，先隔离再非交互，先小模型试通路再换贵模型打仗。会话一乱就新建，工具一空转就取消，权限一越界就拒绝，上下文一满就压缩或换桌。文档、源码说明、设计书、优化方案四份文件分工不同：本文件只解决“怎么用”。把口诀贴在显示器旁边，比把说明书通读十遍更管用。若你只记住一件事，请记住：助手的每一步成功都建立在你的工作区和 Git 上，责任始终在操作者，而不在状态栏那个会报费用的名字。


### 27.17 功能对照速查（避免来回翻章）

交互启动用 `opencode`，指定目录用 `-c`，调试用 `-d`，版本用 `-v`。非交互用 `-p`，脚本加 `-q`，机器可读加 `-f json`。聊天里发送用编辑器快捷键，取消用 `Ctrl+X`，新会话 `Ctrl+N`，切会话 `Ctrl+S`，命令 `Ctrl+K`，模型 `Ctrl+O`，主题 `Ctrl+T`，附件 `Ctrl+F`，日志 `Ctrl+L`，帮助 `Ctrl+H`，退出 `Ctrl+C`。权限一次允许 `a`，本会话记住 `A`，拒绝 `d`。项目记忆靠 OpenCode.md 与 contextPaths。压缩靠命令或自动。工具层面：找文件 glob，找内容 grep，看目录 ls，读文件 view，改一处 edit，覆盖 write，大块 patch，跑命令 bash，网络 fetch，公网示例 sourcegraph，报错 diagnostics，外包搜索 agent，其余看 MCP 名。配置放家目录或项目 `.opencode.json`，密钥优先环境变量。数据在 `.opencode/opencode.db`。本树已归档，新功能看 Crush。把这一段当作封面背后的速查纸，细节仍以各章为准。若速查与界面不一致，以帮助对话框当时列出的绑定为准，因为那是程序实时生成的，比任何静态说明书都新。


### 27.18 写给不愿通读长文的人

你只需要五步。第一，在仓库根配置好一家模型的密钥。第二，运行 `opencode` 先问只读问题，确认它能找到文件。第三，把任务写成“改什么、怎么验证、不许做什么”。第四，每个写文件和 bash 的弹窗都读参数，看不懂就拒绝。第五，自己跑测试并 `git diff`，满意再提交。其余功能都是这五步的加速器：命令面板是常用提示的抽屉，MCP 是外接器官，LSP 是编译器耳目，压缩是给健忘同事写的备忘录，非交互是把五步里的弹窗全部自动按同意——所以脚本里更要小心。若五步中任何一步让你觉得失去控制，停下来，这比追问第一百零一个技巧更重要。工具会过时，这五步不会。把控制权留在自己手里，助手才能成为助手而不是事故来源。遇到不确定的行为，回到本说明书用搜索找关键词，而不是连续加戏让模型自己猜。耐心比更强的模型更便宜。


### 27.19 收尾声明

本使用说明书的篇幅用于把每个按钮、每个字段、每个失败现场都写到可执行，而不是凑字数。若你是审计人员，请结合设计书核对权限与数据流；若你是新用户，从第 2 章安装走到第 3 章发出第一句只读提问即可；若你是平台负责人，第 20 章安全和优化方案里的沙箱建议比再学一个快捷键更要紧。文档会与归档代码一起冻结，界面若与文字冲突，以正在运行的帮助对话框和源码为准。感谢你把终端里的一位冲动实习生管教成可用的同事。使用愉快，提交谨慎，权限认真，测试常在。


无论你从哪一章读起，请把“工作区是真相、Git 是后悔药、权限是刹车”三句话带进每一次启动。功能可以忘，这三句不能忘。其余快捷键用帮助对话框现场查即可。写到这里，使用说明书该说的都已说完。


---

## 28. 附录：推荐工作流备忘

1. 仓库根目录启动；确认 Git 干净或可丢弃。  
2. 配好密钥与可选 LSP。  
3. 第一次：Initialize Project，审查 OpenCode.md。  
4. 小步任务：一次一个目标，写清测试命令。  
5. 认真看每一次写权限和 bash。  
6. 结束后自己 `git diff`，不要盲目提交。  
7. token 高了再 compact 或新开会话。  
8. 脚本场景才用 `-p`，并视提示为高权限用户。

---

---

*本使用说明书依据当前仓库源码与 README 整理，面向日常操作。架构级细节见 `1DESIGN.md`，源文件清单见 `4SOURCE.md`，演进建议见 `3SOLUTION.md`。*
